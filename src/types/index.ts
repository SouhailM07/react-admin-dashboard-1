export interface sidebarLinks {
  title: string;
  links: { img: any; txt: string; link?: string }[];
}

export interface myBox {
  Bg: string;
  img: any;
  n: string;
  label: string;
  percent: string;
  Color: string;
}
